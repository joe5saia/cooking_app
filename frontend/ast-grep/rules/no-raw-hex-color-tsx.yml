id: no-raw-hex-color-tsx
language: Tsx
message: 'Avoid raw hex colors; use tokens (var(--color-...)) or theme constants.'
severity: error
rule:
  kind: string
  regex: "^['\\\"]#([0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})['\\\"]$"
